# AGENTS.md — 開発規約（Codex向け・全AIエージェント共通）

このリポジトリで作業するAIエージェントは、**作業開始前に必ずこのファイルを読み、全ルールに従うこと**。
（Claude向けの補足は `CLAUDE.md` にあるが、本ファイルが共通ルールの原本である）

## プロジェクト概要

中国人向け特定技能求人サイト「樱聘 YingPin」。運営は株式会社パートナー（有料職業紹介事業 許可番号 11-ユ-301340）、入社後支援はパートナー協同組合（登録支援機関）。
- ターゲット: 日本で働きたい／働いている20〜30代の中国人（スマホ前提）
- 会員側UI言語: 中国語（簡体字）デフォルト＋日本語切替。管理画面: 日本語のみ
- 全体計画: `docs/beta-plan.md`／作業チケット: `docs/tasks.md`（GitHub Issuesと同期）

## リポジトリ構成

```
├── index.html ほか *.html   # 完成済みモック（β版の見た目の正）。原則変更しない
├── assets/                  # モックのCSS/JS。デザイントークンと日中辞書の原本
├── tools/                   # モックの1ファイル版ビルド
├── docs/                    # 計画書・チケット
├── app/                     # β版本体（Next.js）。Phase 1 で新設される
├── AGENTS.md / CLAUDE.md    # 本規約
└── README.md
```

## 技術方針

- **スタック**: Next.js（App Router）+ TypeScript + Supabase（DB/Auth/RLS）+ Vercel + Resend
- **デザイン**: モックが唯一のデザイン正。`assets/css/style.css` のCSS変数（ブルー基調 `--primary:#1E6FFF` 等）を必ず使い、独自の色・角丸・影を発明しない
- **文言**: すべて辞書キー経由（ハードコード禁止）。原本は `assets/js/i18n.js` → `app/src/i18n/` に移行
- **禁止**: Google系外部サービス（フォント/解析/地図）は中国アクセス配慮のため使用禁止。外部CDN読み込みも避ける
- **認証**: 電話番号＋パスワード（SMSなし）。`本人確認済み` フラグはスタッフが管理画面で操作する設計
- **依存パッケージの追加は最小限**。Issueに明記がないパッケージを入れたい場合は、勝手に追加せずPR説明に提案として書く

## 作業ルール

1. **Issue単位で作業する**。Issueに書かれた「触る範囲」のフォルダ以外は変更しない（他エージェントとの衝突防止）
2. **変更禁止（Claude専任）**: `app/supabase/`（DBスキーマ・RLS）、`AGENTS.md`、`CLAUDE.md`、`.github/`、既存モック（`*.html`・`assets/`・`tools/`）。バグを見つけた場合はPR説明に報告のみ
3. **ブランチ**: `feature/<Issue番号>-<短い英語名>`（例: `feature/8-admin-jobs-crud`）。`main` および `claude/` で始まるブランチへ直接pushしない
4. **PR必須**: マージはユーザー（人間）がPR画面で行う。PR説明には「目的／変更点／確認方法（画面の開き方・試す手順）」を日本語で書く
5. **コミットメッセージ**: 日本語で1行要約（例: `管理画面: 求人一覧と公開切替を追加`）
6. **動作確認**: `app/` では `npm run lint && npm run build` を通してからPRを出す。UIはスマホ幅（390px）で崩れないことを確認
7. **秘密情報**: APIキー・パスワード・実在個人情報をコードやコミットに含めない。環境変数は `.env.example` にキー名のみ追記
8. **質問・相談**: 不明点は実装を止めず、PR説明または該当Issueのコメントに日本語で書く（ユーザーが橋渡しし、Claudeが回答する）
9. **勝手なリファクタ禁止**: 指示された範囲の外の「ついで修正」はしない。提案はPR説明に書く

## 品質基準（PRがレビューを通る条件）

- モックと見た目が一致している（デザイントークン使用・スマホ幅で崩れない）
- 文言がすべて辞書キー経由で、中国語/日本語の両方が入っている
- ログイン必須ページが未ログインでガードされている
- `lint`/`build` が通る。console.logの残骸がない
