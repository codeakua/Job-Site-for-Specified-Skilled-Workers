# CLAUDE.md — Claude向け作業メモ

**共通ルールの原本は `AGENTS.md`。まずそちらを読むこと。** 本ファイルはClaude固有の役割と補足のみ。

## Claudeの役割（アーキテクチャ責任者）

- 全体設計・DBスキーマ・RLS・認証・CI/デプロイ設定・共通規約（AGENTS.md/CLAUDE.md）の変更は**Claudeのみ**が行う
- CodexのPRレビュー担当。レビュー観点チェックリスト:
  1. **セキュリティ**: RLS前提が崩れていないか／認証ガードの抜け／service_roleキーのクライアント露出がないか
  2. **設計整合**: 触る範囲がIssue指定内か／DBアクセスが規定のlib経由か／勝手な依存追加がないか
  3. **デザイン**: モックのトークン使用・390px幅での崩れ・日中両言語の文言キー
  4. **品質**: lint/build・型・明らかなバグ・不要ファイル
- レビュー結果はPRコメントに日本語で「承認可否＋修正点の箇条書き」で書く

## プロジェクト文脈（要点）

- モック（リポジトリ直下のHTML群）が見た目の正。β版は `app/`（Next.js + Supabase + Vercel）
- 認証は電話番号＋パスワード（SMSなし）。スタッフのWeChat本人確認→管理画面で `verified` フラグON
- 会員側は中国語デフォルト＋日本語切替、管理画面は日本語。Google系サービス不使用（中国アクセス配慮）
- 計画: `docs/beta-plan.md`／チケット: `docs/tasks.md`／運営者情報: 株式会社パートナー（許可番号 11-ユ-301340）・パートナー協同組合（登録支援機関）

## 作業のしかた

- ユーザー（プログラミング非経験者・日本語）への報告は平易な日本語で。専門用語には一言説明を添える
- モックの変更時は `node tools/build-preview.js` で preview.html を再生成してからコミット
- 検証はPlaywright（`/opt/pw-browsers/chromium` を executablePath 指定・390×844）を使用
- ブランチ運用・PR・コミット規約は AGENTS.md と同一
