> # ✅ この作業は 2026-07-28 に完了しました。このプロンプトはもう使いません
>
> 新プロジェクト **`jdiybvtytrdkuxsiddic`（東京 `ap-northeast-1`）** へ移設済みです。実施内容・実地で判明した相違点・残作業は **`docs/progress.md` §24** にまとめてあります。以下は**着手前に書いた指示文で、履歴として残しています**。
>
> **残っているのは次の2つだけです。**
> 1. オーナー: 旧プロジェクト `jqevswrbdbmxifauqhfi`（豪州）の削除（数日後）
> 2. Claude: **削除完了後に**法務書類の保管国をオーストラリア→日本へ書き換え、Word/PDFを再生成
>
> ⚠️ **下の「前提」の最後の項目（外的環境の把握が軽くなる、という記述）は誤りでした。** この義務は外国のサーバに保存する場合だけでなく**外国の会社に取扱いを委託している場合**も対象のため、東京に移しても委託先が米国法人である限りアメリカは残ります。訂正の詳細は §24 と `docs/ops/supabase-region-migration-guide.md` §2 を参照してください。

---

Supabase（会員データの保管先）をオーストラリアから東京へ移す作業を進めたいです。手順書は作ってあるので、それに沿って私の操作を案内してください。

まず `docs/progress.md` を読んでください（このプロジェクトの現状がすべて書いてあります）。特に **§21・§22** が直近の経緯です。続いて `CLAUDE.md` と `AGENTS.md`、そして手順書 **`docs/ops/supabase-region-migration-guide.md`** を読んでください。

## 前提（調査済みの事実。再調査は不要です）

- 現在のSupabaseプロジェクトは **`ap-southeast-2` / Oceania (Sydney)＝オーストラリア**。管理画面（Project Settings → General）で確認済みです。プロジェクトIDは `jqevswrbdbmxifauqhfi`
- **Supabaseにはリージョンを後から変更する機能がありません。** 東京に新しいプロジェクトを作って切り替えるのが唯一の方法です
- 貼り付けるSQLは **`app/supabase/setup.sql`**。`app/supabase/migrations/` の4ファイルと**完全に一致していることを確認済み**です（2026-07-27時点）
- 認証のセキュリティ設定（パスワード最低文字数・漏洩パスワード拒否・レート制限）と、スタッフのログイン情報は**新プロジェクトに引き継がれません**。やり直しが必要です
- ⚠️ 東京へ移しても、個人情報保護法の「外国にある第三者への提供」（第28条）の話は**消えません**。該当性はサーバーの設置国ではなく**提供先の法人がどこの国の会社か**で決まり、Supabase, Inc. は米国法人だからです。軽くなるのは第32条「外的環境の把握」の方です。この理解は弁護士確認論点 A-1 に記載済み

## あなた（Claude）の役割

**SupabaseとVercelの画面操作は私（オーナー）が行います。あなたはログインできません。** あなたの役割は、手順書の各ステップを1つずつ案内し、私が見ている画面の内容を伝えたらそれを読んで次を指示することです。

- 私はプログラミング未経験です。専門用語には一言説明を添えてください
- 私が画面のエラーやスクリーンショットを送ったら、それを読んで対処を教えてください
- **一度に全ステップを説明せず、1ステップずつ進めてください**

## 進め方

手順書のステップ1から順に進めます。まず、作業前の確認として

1. 管理画面 `/admin/members` に**実在の求職者が何人いるか**（テスト登録だけなら作り直しが最も簡単。実在の会員が多数いる場合はデータ移行の手順に切り替える必要があります）

を私に尋ねるところから始めてください。

## 移設が完了したら、あなたが行う後片付け

1. **法務文書の保管国をオーストラリア→日本（東京）に変更**して、Word/PDFを作り直す
   - `docs/legal/privacy-draft.md` の第6条・第7条（移転先の国名・当該国の制度の説明。オーストラリアの説明は不要になります）
   - `docs/legal/overview.md` の「4. 個人情報の流れと技術構成」（2026-08-06 に `cover-letter.md` から改称・作り直し）
   - `docs/legal/lawyer-checklist.md` の論点 A-1 の表
   - ⚠️ **アメリカ（Vercel・Resend）の記述は残します。** また、Supabase, Inc. が米国法人であることに由来する第28条の整理も残します
   - 再生成: `node tools/docgen/build-legal.js`
   - ⚠️ 新しいコンテナでは `libreoffice-writer` と日本語フォント（`fonts-ipafont-mincho`）と `/root/.config/fontconfig/fonts.conf` が必要です。詳細は `docs/progress.md` §21 の「生成環境」
2. `docs/ops/supabase-auth-policy-guide.md` と `docs/progress.md` の**古いプロジェクトID `jqevswrbdbmxifauqhfi` を新しいIDに差し替え**
3. `docs/progress.md` に §23 として結果を記録
4. コミットしてプッシュ

## 注意

- **旧プロジェクトは、新しい環境で動作確認が全部通るまで削除しないでください。** 切り戻せる状態を保ちます
- 環境変数を変えただけではサイトに反映されません。**Vercelでの再デプロイが必須**です（手順書のステップ5-3）
- 「Confirm email」は**絶対にONにしないでください**。このサイトは架空のメールアドレス（`@phone.yingpin.app`）を使うため、ONにすると新規登録が全員できなくなります
