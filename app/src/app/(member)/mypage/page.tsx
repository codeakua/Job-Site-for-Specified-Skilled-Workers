import { Placeholder } from "@/components/Placeholder";

// T-06（Codex）で本実装。マイページ（登録情報・応募履歴・設定）。
// 暫定でログアウトボタンを表示（β動作確認用）。
export default function MypagePage() {
  return <Placeholder titleKey="my.title" ticket="T-06" showLogout />;
}
