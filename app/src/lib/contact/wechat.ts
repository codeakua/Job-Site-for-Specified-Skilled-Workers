/**
 * WeChat（微信）のサポート窓口。会員 → スタッフの主要な連絡導線。
 *
 * ⚠️ **QRコードを自前で生成してはいけない。**
 * WeChatの「友だち追加」QRは、WeChat IDの文字列をQRにエンコードしたものではなく、
 * アカウントごとにWeChatが発行する `https://u.wechat.com/…`（国際版）または
 * `http://weixin.qq.com/r/…`（中国版）のデータを持つ。
 * IDからは計算できないため、`WECHAT_ID` を正しくQRエンコードしても、
 * WeChatで読み取ったときに「ただの文字列」として表示されるだけで友だち追加にならない。
 *
 * したがってQRを出す唯一の正しい方法は、**オーナーがWeChatアプリから書き出した公式の画像**
 * （「我」→「二维码名片」→ 画像を保存）を `app/public/` に置き、そのパスを
 * `WECHAT_QR_SRC` に設定すること。
 *
 * 画像がある場合の利点: WeChatの内蔵ブラウザでは `<img>` のビットマップに対して
 * 「长按识别二维码」（長押しで読み取り）が効く。微信群・朋友圈からの流入では
 * これが最短の導線になる。※インラインSVGでは効かないので必ず画像で置くこと。
 */

/**
 * 窓口に使っているアカウント（2026-08-01 オーナー確認済み）:
 *
 *   表示名 : 招聘支援指南（Region: Japan）
 *   内部ID : wxid_worwvvi3kdml12
 *
 * ⚠️ **オーナーの個人アカウントとは別のアカウント。**
 * QRを差し替えるときは必ずこのアカウントにログインして書き出すこと。
 * 個人アカウントから書き出すと、求職者が別人を友だち追加することになる。
 * 確認は「別端末でQRを読み、**招聘支援指南 と表示されること**」まで見る
 * （別アカウントのQRでも「友だちに追加」の画面自体は出るため）。
 */

/**
 * 利用者が**検索できる**「微信号」。設定されていないあいだは `null`。
 *
 * 🔴 **`wxid_…` を入れてはいけない。**
 * `wxid_` で始まる文字列はWeChatが内部で使う識別子で、アプリの検索欄
 * （「+」→ 友だちを追加 → 検索）に入れても**該当なしになる**。
 * 利用者に検索させられるのは、本人が「我 →（自分の名前をタップ）→ 微信号」で
 * 設定した文字列だけ。
 *
 * 存在しない・検索できないIDを載せると、利用者は探して見つからず離脱する。
 * これはダミーQRを載せていたのと同じ「行き止まり」なので、**確認できるまでは
 * `null` のままにしてQRだけを出す。**
 *
 * 微信号が設定されたら、ここに文字列を入れるだけでID表示・コピー・検索手順が復活する
 * （手順は `docs/ops/wechat-qr-guide.md`）。
 */
export const WECHAT_ID: string | null = null;

/**
 * 公式QR画像のパス（`app/public/` からの絶対パス）。`null` のあいだはQRを表示しない。
 *
 * 現在の画像はオーナー提供の友だち追加QR（`https://u.wechat.com/…` を指す。
 * JPEGの圧縮ノイズを落として2値のPNGに変換済み・約3.6KB）。
 *
 * ⚠️ 差し替えるときは、**パスの変更と画像の差し替えを必ず同じコミットで行う**こと。
 * `<img>` は素のタグなのでパスだけ変えてもビルドは通り、本番で画像が割れる。
 * `tools/check-wechat-qr.js` がこの不整合を見張っていて、CIで落ちる。
 */
export const WECHAT_QR_SRC: string | null = "/wechat-qr.png";
